function richWhite() {
    let inputs = ['Chardonnay', 'Chenin Blanc', 'Viognier'];
    FindWines('varietal', inputs);
}

function lightWhite() {
    let inputs = ['Sauvignon Blanc', 'Albarino', 'Chablis', 'Rose'];
    FindWines('varietal', inputs);
}

function sweetWhite() {
    let inputs = ['Riesling', 'Gewurztraminer'];
    FindWines('varietal', inputs);
}

function sparkling() {
    let inputs = ['Sparkling', 'Champagne'];
    FindWines('varietal', inputs);
}

function mediumRed() {
    let inputs = ['Grenache', 'Chianti', 'Barbera', 'Sangiovese', 'Cabernet Franc', 'Blend'];
    FindWines('varietal', inputs);
}

function bigFruitRed() {
    let inputs = ['Red Bordeaux Blend', 'Merlot', 'Petite Syrah', 'Syrah', 'Red Zinfandel', 'Mencia', 'Amarone', 'Cabernet Sauvignon'];
    FindWines('varietal', inputs);
}

function bigDryRed() {
    let inputs = ['Napa Cabernet Sauvignon', 'Tempranillo', 'Malbec', 'Mourvede', 'Nebbiolo', 'Rhone', 'Super Tuscan'];
    FindWines('varietal', inputs);
}

function lightRed() {
    let inputs = ['Pinot Noir', 'Gamay', 'Malbec', 'Petite Syrah', 'Mourvede', 'Nebbiolo'];
    FindWines('varietal', inputs);
}

function sweetDessert() {
    let inputs = ['Lambrusco', 'Port', 'Sauternes', 'Sherry', 'Tokaji', 'Zweigelt'];
    FindWines('varietal', inputs);
}

function redDessert() {
    let inputs = ['Port', 'Sherry'];
    FindWines('varietal', inputs);
}

function sauternes() {
    let inputs = ['Sauternes'];
    FindWines('varietal', inputs);
}

